{
  "name": "date-night-app",
  "version": "1.0.0",
  "private": true,
  "description": "Date Night - pick movies and TV shows to watch together",
  "scripts": {
    "dev": "concurrently \"npm run dev --prefix server\" \"npm run dev --prefix client\"",
    "install:all": "npm install --prefix server && npm install --prefix client"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
